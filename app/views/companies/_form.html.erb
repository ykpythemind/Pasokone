<%= form_with(model: company, local: true) do |form| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :company_name %>
  </div>

  <div class="field">
    <%= form.label :type_of_industry %>
    <%= form.text_field :type_of_industry, id: :company_type_of_industry %>
  </div>

  <div class="field">
    <%= form.label :employee_number %>
    <%= form.number_field :employee_number, id: :company_employee_number %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :company_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
