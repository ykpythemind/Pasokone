<%= form_with(model: person, local: true) do |form| %>
  <% if person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(person.errors.count, "error") %> prohibited this person from being saved:</h2>

      <ul>
      <% person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :person_name %>
  </div>

  <div class="field">
    <%= form.label :company_name %>
    <%= form.text_field :company_name, id: :person_company_name %>
  </div>

  <div class="field">
    <%= form.label :department %>
    <%= form.text_field :department, id: :person_department %>
  </div>

  <div class="field">
    <%= form.label :position %>
    <%= form.text_field :position, id: :person_position %>
  </div>

  <div class="field">
    <%= form.label :relation %>
    <%= form.text_field :relation, id: :person_relation %>
  </div>

  <div class="field">
    <%= form.label :staff_id %>
    <%= form.number_field :staff_id, id: :person_staff_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
